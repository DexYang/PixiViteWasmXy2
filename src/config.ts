
type Config = {
  ui_prefer: string,
  ui_duration: number,
  action_duration: number,
  walk_speed: number,
  run_speed: number
};

export default {
    ui_prefer: "红木",
    ui_duration: 100,
    action_duration: 100,
    walk_speed: 2,
    run_speed: 4
} as Config
